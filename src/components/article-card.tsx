import Image from "next/image";
import Link from "next/link";
import type { Article } from "@/lib/news/types";

export function ArticleCard({ article, featured = false }: { article: Article; featured?: boolean }) {
  return <article className={featured ? "group overflow-hidden rounded-2xl bg-[#17211b] text-[#fcfcf8]" : "group border-b border-[#d9ded5] pb-6"}>{article.imageUrl ? <Link href={`/article/${article.id}`} className={`relative block overflow-hidden ${featured ? "aspect-[16/10]" : "aspect-[16/9]"}`}><Image src={article.imageUrl} alt="" fill unoptimized sizes={featured ? "(max-width: 768px) 100vw, 60vw" : "(max-width: 768px) 100vw, 30vw"} className="object-cover transition duration-500 group-hover:scale-105" priority={featured} /></Link> : null}<div className={featured ? "p-6 md:p-8" : "pt-4"}><div className="mb-3 flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.16em] text-[#e86e42]"><span>{article.category}</span><span className="text-[#9ba59d]">•</span><span className={featured ? "text-[#b9c6bd]" : "text-[#7a847d]"}>{article.sourceName}</span></div><Link href={`/article/${article.id}`}><h2 className={`display-face font-bold leading-[1.05] ${featured ? "text-3xl md:text-5xl" : "text-2xl"}`}>{article.title}</h2></Link><p className={`mt-3 text-sm leading-6 ${featured ? "text-[#c5d0c9]" : "text-[#667169]"}`}>{article.description}</p><div className={`mt-5 text-xs ${featured ? "text-[#aab8af]" : "text-[#879189]"}`}>{new Date(article.publishedAt).toLocaleDateString("en-US", { month: "short", day: "numeric" })}</div></div></article>;
}